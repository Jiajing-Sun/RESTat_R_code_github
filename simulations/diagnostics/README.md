# Diagnostics

This folder contains current diagnostic code.

- `compute_rsms_offdiag_diagnostic.R` computes the RSMS off-diagonal diagnostic used to assess the approximate
  diagonalization condition.

The script expects access to the simulation modules and, for the empirical diagnostic, the TickData `SPX.csv` file.
Those data are not included in this GitHub code release.
